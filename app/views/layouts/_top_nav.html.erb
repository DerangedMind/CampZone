<nav class="navbar navbar-default nav-pills">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#user-dropdown" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span><img class="nav-avatar" src="<%= Faker::Avatar.image("avatar", "30x30", "png", "set4") %>"> <%= current_user.first_name %> <span class="glyphicon glyphicon-chevron-down"></span></span>
      </button>
      <a class="navbar-brand" href="#">CampZone</a>
    </div>
    <div class="collapse navbar-collapse" id="user-dropdown">
      <ul class="nav navbar-nav pull-right">
        <% if current_user %>
          <li class="navbar-text" id="hide-welcome">Welcome, <%= current_user.first_name %></li>
          <li><a class="nav-link" href="/logout">Logout</a></li>
        <% else %>
          <li><a href="/login">Login</a></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
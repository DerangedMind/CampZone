<section class="col-sm-offset-1 col-sm-10 card-list">
  <% objects.each_with_index do |object, i| %>
    <article class="col-md-6">
      <%= render "layouts/#{type}_card", objects: objects, object: object, i: i, role: role %>
    </article>
  <% end %>
</section>
